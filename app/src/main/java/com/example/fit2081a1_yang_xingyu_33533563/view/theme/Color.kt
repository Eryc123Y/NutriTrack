package com.example.fit2081a1_yang_xingyu_33533563.view.theme

import androidx.compose.ui.graphics.Color

// Light theme colors - Updated for a more vibrant, health-focused palette
val Leaf = Color(0xFF4CAF50)  // Retain the original green, but use more variations elsewhere
val SoftBlue = Color(0xFF42B0FF)  // Brighter blue for a fresh feeling
val Mint = Color(0xFF66BB6A)  // Brighter mint color

// Brighter background colors
val LightBackgroundWhite = Color(0xFFF8FDF9)  // Very light greenish background, more refreshing
val LightSurfaceWhite = Color(0xFFFFFFFF)  // Pure white surface
val LightOnPrimaryBlack = Color(0xFFFFFFFF)  // Keep white on primary color
val LightOnSurfaceBlack = Color(0xFF2E3A2F)  // Deeper gray for higher contrast

// Dark theme colors - Softer dark theme
val DarkLeaf = Color(0xFF43A047)  // Brighter dark green
val DarkSoftBlue = Color(0xFF2196F3)  // Brighter dark blue
val DarkMint = Color(0xFF4CAF50)  // Brighter mint color

// More friendly dark backgrounds
val DarkBackgroundBlack = Color(0xFF0F1E12)  // Dark greenish background
val DarkSurfaceBlack = Color(0xFF1B2E1F)  // Dark surface with greenish tone
val DarkOnPrimaryWhite = Color(0xFFFFFFFF)  // Keep white on dark green primary
val DarkOnSurfaceWhite = Color(0xFFF0F0F0)  // Brighter gray for better readability

// Vibrant colors for healthy food categories
val Fruits = Color(0xFFFF5252)       // Brighter red, represents fruits
val Vegetables = Color(0xFF66BB6A)   // Fresh green, represents vegetables
val Grains = Color(0xFFFFB300)       // Warm gold, represents grains
val Proteins = Color(0xFF8E24AA)     // Deep purple, represents proteins
val Dairy = Color(0xFFE0F7FA)        // Light blue, represents dairy
val Hydration = Color(0xFF29B6F6)    // Refreshing blue, represents water/hydration

// Status colors - More vivid feedback colors
val Success = Color(0xFF4CAF50)      // Brighter green for success
val Warning = Color(0xFFFF9800)      // Brighter orange for warning
val Error = Color(0xFFF44336)        // Brighter red for error

// Additional accent colors for highlights and interactive elements
val AccentTeal = Color(0xFF009688)   // Fresh teal
val AccentAmber = Color(0xFFFFB300)  // Warm amber
val AccentCyan = Color(0xFF00BCD4)   // Bright cyan
val AccentLime = Color(0xFFCDDC39)   // Vibrant lime